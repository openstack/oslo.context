============
Installation
============

At the command line::

    $ pip install oslo.context

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv oslo.context
    $ pip install oslo.context